{
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && browserify -t browserify-css index.js -o bundle.js",
    "postinstall": "npm dedupe"
  },
  "dependencies": {
    "phosphor-dockpanel": "^0.9.0",
    "phosphor-tabs": "^0.9.5",
    "phosphor-widget": "^0.9.9"
  },
  "devDependencies": {
    "browserify": "^11.1.0",
    "browserify-css": "^0.7.1",
    "typescript": "^1.6"
  }
}
